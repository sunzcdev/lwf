[project]
name = "lwf"
version = "0.1.0"
description = "Capability-driven workflow framework — GHA + local hybrid"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0", "jsonschema>=4.0"]

[project.scripts]
lwf = "lwf.cli:main"
